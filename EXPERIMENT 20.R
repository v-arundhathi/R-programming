# Create an empty plot
plot(0, type = "n", xlim = c(0, 10), ylim = c(0, 20), xlab = "X-axis", ylab = "Y-axis", main = "Empty Plot")

# Create an empty plot with specified axes limits
plot(0, type = "n", xlim = c(-5, 5), ylim = c(-10, 10), xlab = "X-axis", ylab = "Y-axis", main = "Empty Plot with Axes Limits")
